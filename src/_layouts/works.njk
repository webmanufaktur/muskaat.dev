---
layout: base
---

<h1>{{ title }}</h1>
<p>{{ project }}</p>
<div class="desc">
  <p>{{ desc }}</p>
</div>
<ul>
  {% for topic in topics %}
  <li>
    {{ topic }}
  </li>
  {% endfor %}
</ul>
<ul>
  {% for item in tasks %}
  <li>
    {% for label, desc in tasks[loop.index0] %}
    <div>
      <strong>{{ label }}</strong> {{ desc }}
    </div>
  </li>
  {% endfor %} {% endfor %}
</ul>
<hr />
{{ colorBrand }}

<hr />
{{ colorText }}
<hr />
{{ logo }}
<hr />
{{ screenMobile }}
<hr />
{{ screenDesktop }}
